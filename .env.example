# NewsChat does not use environment variables.
# All settings (API URL, model, API key) are stored in chrome.storage.local via the options page.
# See README.md → Configuration for setup instructions.
